# marketplace-preorders

Deploy repo for the **preorders** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs preorders --push`.

Coolify: base_directory `/`, dockerfile `/apps/preorders/Dockerfile`.
